#!/bin/bash

mkdir -p ~/.claude
cp ./claude/settings.json ~/.claude/

mkdir -p ~/.claude-code-router
cp ./claude-code-router/config.json ~/.claude-code-router/

mkdir -p ~/.kimi
cp ./kimi/config.toml ~/.kimi/
